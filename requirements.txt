# Core (required)
gradio>=4.0
edge-tts>=6.0
pdfplumber>=0.10
ebooklib>=0.18
beautifulsoup4>=4.12
trafilatura>=1.6
pydub>=2.0

# Optional: Kokoro offline TTS (~80MB model on first use)
# kokoro-onnx soundfile

# Optional: spaCy for character detection (~12MB model)
# spacy
# python -m spacy download en_core_web_sm
